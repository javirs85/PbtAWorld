@using static ScumAndVillainy.Controls.SkewedHeader
<div class="Tick @(IsUsable?"":"NotUsable") @(IsInverted?"IsInverted":"") @Color.ToString()" style="@(ForceSize != 0?$"height:{ForceSize}px;":"")" @onclick=Clicked>
	<div class=" @(IsTicked?"Ticked":"")">

	</div>
</div>

@code {
	[Parameter] public AvilableColors Color { get; set; } = AvilableColors.Blue;
	[Parameter] public bool IsTicked { get; set; }
	[Parameter] public bool IsInverted { get; set; }
	[Parameter] public EventCallback Clicked { get; set; }
	[Parameter] public float ForceSize { get; set; } = 0;
	[Parameter] public bool IsUsable { get; set; } = true;
}
